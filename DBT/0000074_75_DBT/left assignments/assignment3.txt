

1.


select ename from emp where job = 'manager';


2.

select ename from emp where sal >= 3000


3.

select ename from emp where ename like '_a%';

4.

select empno, ename, hiredate, job from emp where deptno = 10;


5.


select * from emp where sal > 2000;


6.

select ename from emp where deptno in (10, 20) and ename like 'a%b';

7.

select * from emp where hiredate = '1990-12-21';

8.

select empno, ename, job, (sal * 12) as annual_salary from emp where deptno = 10;

9.

select * from emp where (sal * 12) > 25000 order by sal desc;

10.

select ename from emp where ename like '%f%';

11.

select empno, ename, job, (sal * 12) as annual_salary from emp where deptno = 10;

12.

select * from emp where (sal * 12) > 25000 order by sal desc;

13.

select ename from emp where ename like '%f%';

14.

select distinct deptno from emp order by deptno desc;

15.

select ename, empno, job, sal from emp where sal between 1000 and 2000;

16.

select ename, (sal * 12 * 0.011) as yearly_bonus from emp;

17.

select ename, job, deptno from emp where deptno in (30, 40);

18.

select empno, ename from emp where job in ('clerk', 'manager') and sal > 1500;

19.

select ename, (sal * 12) as annual_salary, comm as "commission earned" from emp where sal > ifnull(comm, 0) order by sal desc, ename asc;

20.

select ename, job from emp where job in ('salesman', 'manager') and ename like '%s';


21.

select ename from emp where sal > 1000 and sal <= 2200;

22.

select ename, sal from emp where job = 'clerk';

23.

select deptno, ename from emp where sal < 2500;

24.

select empno, ename from emp where job = 'president';

25.

select ename from emp where job in ('clerk', 'analyst', 'salesman');

26.

select ename from emp where ename like 'b%' or ename like 'm%';

27.

select * from emp order by ename asc;

28.

select * from emp where comm > (sal * 0.05);

29.

select ename, deptno from emp where empno = 7566;

30.

select ename as name, sal as "monthly salary" from emp where sal > 1500 and deptno in (10, 30);

31.

select ename, sal, comm from emp where comm is not null order by sal desc, comm desc;

32.

select ename from emp where ename like '__a%';

33.

select ename from emp where (ename like '%ls%ls%') and (deptno = 30 or mgr = 7782);


34.

select ename, job, sal from emp where job in ('clerk', 'analyst') and sal not in (1000, 3000, 5000);

35.

select ename, sal, comm from emp where comm > (sal * 1.1);

36.

select empno, ename, (ifnull(bonusid, 0) + sal*12 + ifnull(comm, 0)) as gross_salary from emp where deptno in (20, 30) order by empno asc;

37.

select empno, ename, sal, round(sal * 1.15) as "new salary" from emp;

38.

select concat(upper(substring(ename, 1, 1)), lower(substring(ename, 2))) as formatted_name, length(ename) as name_length from emp where ename like 'j%' or ename like 'a%' or ename like 'm%';


39.

select ename, ifnull(cast(comm as char), 'no commission') as commission_amount from emp;

40.

select customer_name as "customers name", sales_amount as "sales amount", round(sales_amount * 0.08) as "approximate profit earned" from sales_table;

41.

select * from sales_table where year(sale_date) = 1999 and quarter(sale_date) = 1; select * from sales_table where product_name = 'sp tennis racket';

42.

select concat(upper(substring(c_name, 1, 1)), lower(substring(c_name, 2))) as name, repid, concat('$', lpad(round(credit_limit * 1.25), 9, ' ')) as "new credit limit" from customer;


43.

select ename, case deptno when 10 then 'belongs to dept. 10' when 20 then 'belongs to the dept. 20' else 'neither 10 nor 20.' end as department_status from emp;

44.

select ename, upper(substring(ename, 1, 1)) as "abbreviated name", ((sal + ifnull(comm, 600)) * 12) as annual_compensation from emp;

45.

select order_id, total_value, case commission_plan when 'a' then 'anonymous' when 'b' then 'business' when 'c' then 'casual' else 'commission not allowed' end as commission_status from orders;

46.

select * from emp where city = 'boston';

47.

select ename from emp where city like '_a%' or city like '_h%';

48.

select e.ename from emp e join dept d on e.deptno = d.deptno where d.dname = 'sales';

49.

select ename from emp order by ename asc;


50.

select * from emp order by sal desc;

51.

select * from emp order by year(hiredate) asc;


52.

select job, deptno, ename from emp where ename like 'b%' or ename like 'm%';

53.


select * from emp where comm is null and sal <= 3000;
















































