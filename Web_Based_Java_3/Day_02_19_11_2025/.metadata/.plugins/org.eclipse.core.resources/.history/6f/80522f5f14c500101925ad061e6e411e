
public class TestArrAdd {

	public static void main(String[] args) {
		
		
		int []arr = {1,2,3,4,6};
		
		int preSum=0,sum=0;
		for(int i =0;i<arr.length;i++) {			
			sum = getSum(arr,i,preSum);
			preSum += arr[i];
			arr[i]=sum;
			
		}
	}

	private static int getSum(int[] arr, int i, int preSum) {
		
		for(int j = )
		
		return 0;
	}

}
